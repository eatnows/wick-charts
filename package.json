{
  "name": "cinderchart",
  "version": "0.0.1",
  "description": "An open-source financial charting library — WASM (Rust) for compute, Canvas2D for rendering.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "pnpm build && python3 -m http.server 4173",
    "test": "vitest run",
    "test:rust": "cargo test",
    "check:rust": "cargo check --target wasm32-unknown-unknown -p cinderchart-core"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
